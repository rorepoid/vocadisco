[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
crux_core = "0.9"
serde = { version = "1.0", features = ["derive"] }
derive_more = { version = "1.0", features = ["full"] }
thiserror = "1.0"
